class Solution {
public:
    void arrange(vector<int>& arr) {
        int n = arr.size();

        for (int i = 0; i < n; i++) {
            arr[i] += (long long)(arr[arr[i]] % n) * n;
        }

        for (int i = 0; i < n; i++) {
            arr[i] /= n;
        }
    }
};
